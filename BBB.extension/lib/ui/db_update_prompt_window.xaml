<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Kodama — Standards Update Available"
        Width="420"
        SizeToContent="Height"
        WindowStyle="SingleBorderWindow"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        ShowInTaskbar="False"
        Topmost="True"
        Background="#F3F3F3">

    <Window.Resources>
        <SolidColorBrush x:Key="PrimaryColor"       Color="#0078D4"/>
        <SolidColorBrush x:Key="TextPrimaryColor"   Color="#323130"/>
        <SolidColorBrush x:Key="TextSecondaryColor" Color="#605E5C"/>

        <Style TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryColor}"/>
            <Setter Property="FontSize"   Value="12"/>
        </Style>

        <!-- Primary button (Sync Now) -->
        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Background"    Value="#0078D4"/>
            <Setter Property="BorderBrush"   Value="Transparent"/>
            <Setter Property="Foreground"    Value="#FFFFFF"/>
            <Setter Property="FontFamily"    Value="Segoe UI"/>
            <Setter Property="FontSize"      Value="12"/>
            <Setter Property="FontWeight"    Value="SemiBold"/>
            <Setter Property="Height"        Value="32"/>
            <Setter Property="MinWidth"      Value="110"/>
            <Setter Property="Padding"       Value="16,0"/>
            <Setter Property="Cursor"        Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}"
                                CornerRadius="4" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#005A9E"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Secondary button (Later) -->
        <Style x:Key="SecondaryButtonStyle" TargetType="Button">
            <Setter Property="Background"    Value="#FFFFFF"/>
            <Setter Property="BorderBrush"   Value="#C8C6C4"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground"    Value="{StaticResource TextPrimaryColor}"/>
            <Setter Property="FontFamily"    Value="Segoe UI"/>
            <Setter Property="FontSize"      Value="12"/>
            <Setter Property="Height"        Value="32"/>
            <Setter Property="MinWidth"      Value="110"/>
            <Setter Property="Padding"       Value="16,0"/>
            <Setter Property="Cursor"        Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#F3F2F1"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <!-- Outer card -->
    <Border Margin="16"
            Background="#FFFFFF"
            BorderBrush="#E0E0E0"
            BorderThickness="1"
            CornerRadius="6">
        <StackPanel Margin="24,20,24,20">

            <!-- Icon + title row -->
            <StackPanel Orientation="Horizontal"
                        VerticalAlignment="Center"
                        Margin="0,0,0,12">
                <!-- Circular icon badge -->
                <Border Width="36" Height="36"
                        Background="#E3F2FD"
                        BorderBrush="#0078D4"
                        BorderThickness="1.5"
                        CornerRadius="18"
                        Margin="0,0,12,0"
                        VerticalAlignment="Center">
                    <TextBlock Text="↻"
                               FontSize="18"
                               FontFamily="Segoe UI"
                               Foreground="#0078D4"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"
                               Margin="0,1,0,0"/>
                </Border>
                <TextBlock Text="Standards Updated"
                           FontSize="15"
                           FontWeight="SemiBold"
                           VerticalAlignment="Center"/>
            </StackPanel>

            <!-- Body -->
            <TextBlock Text="Updated design standards are available. Syncing now ensures Kodama gives you the most current guidance."
                       FontSize="12"
                       Foreground="{StaticResource TextSecondaryColor}"
                       TextWrapping="Wrap"
                       LineHeight="18"
                       Margin="0,0,0,20"/>

            <!-- Button row -->
            <StackPanel Orientation="Horizontal"
                        HorizontalAlignment="Right">
                <Button x:Name="LaterButton"
                        Content="Later"
                        Style="{StaticResource SecondaryButtonStyle}"
                        Margin="0,0,8,0"/>
                <Button x:Name="SyncNowButton"
                        Content="Sync Now"
                        Style="{StaticResource PrimaryButtonStyle}"/>
            </StackPanel>

        </StackPanel>
    </Border>
</Window>
